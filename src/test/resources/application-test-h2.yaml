spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: jdbc:h2:mem:jira-test;DB_CLOSE_DELAY=-1
    username: jira
    password: JiraRush

  liquibase:
    change-log: db/changelog-test-h2.sql