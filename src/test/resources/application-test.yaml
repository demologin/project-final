spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: ${DATASOURCE_URL:"jdbc:postgresql://localhost:5433/jira-test"}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}