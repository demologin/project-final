spring.cache.type: none
spring:
  init:
   mode: always
  datasource:
    url: jdbc:postgresql://postgres-db-1:5432/jira
    username: jira
    password: JiraRush

app:
  host-url: http://localhost:8080
