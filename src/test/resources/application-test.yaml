spring.cache.type: none
spring:
  sql:
    init:
      mode: always

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:jira;DB_CLOSE_DELAY=-1;
    username: sa
    password: sa

  liquibase:
    changeLog: "classpath:db/changelog.xml"
