spring.cache.type: none
spring:
  init:
    mode: always

  datasource:
    url: jdbc:postgresql://localhost:2346/jira-test
    username: jira
    password: JiraRush

  liquibase:
    drop-first: true
    changeLog: "classpath:db.changelog-test.xml"
    contexts: "test"

